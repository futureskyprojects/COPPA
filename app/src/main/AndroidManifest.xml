<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.vistark.coppa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Fastdroid"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.splash_screen.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.seaport.SeaPortActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.species.SpeciesActivity" />
        <activity android:name=".ui.trip_hostory.TripHistoryActivity" />
        <activity android:name=".ui.category.CategoryActivity" />
        <activity android:name=".ui.auth.AuthActivity" />
    </application>

</manifest>